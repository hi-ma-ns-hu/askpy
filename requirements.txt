# ──  framework ────────────────────────────────────────────────────────────
fastapi==0.136.0
uvicorn==0.44.0
gunicorn==25.3.0
python-multipart==0.0.26

# ── Config ───────────────────────────────────────────────────────────────────
pydantic==2.13.1
pydantic-settings==2.14.1
python-dotenv==1.2.2

# ── LLM (OpenAI) ─────────────────────────────────────────────────────────────
openai==2.32.0
tiktoken==0.12.0
tenacity==9.1.4

# ── Vector store + hybrid retrieval + reranking ──────────────────────────────
qdrant-client==1.18.0
fastembed==0.8.0          # sparse/BM25 encoding for hybrid search (no torch)
cohere==7.0.4             # rerank API

# ── Cache ────────────────────────────────────────────────────────────────────
redis==6.4.0
hiredis==3.3.1

# ── Ingestion (data prep) ────────────────────────────────────────────────────
pandas==2.3.3
beautifulsoup4==4.14.3    # strip HTML from Stack Overflow Q&A
lxml==6.1.0
kagglehub==1.0.2          # pull the dataset from Kaggle via API

# ── Observability (optional — disabled when tokens are empty) ────────────────
logtail-python==0.3.4
sentry-sdk==2.58.0

# ── Testing ──────────────────────────────────────────────────────────────────
pytest==9.0.3
pytest-asyncio==1.3.0
pytest-cov==7.1.0
httpx==0.28.1             # also used by FastAPI TestClient / AsyncClient
